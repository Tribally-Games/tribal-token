{
  "name": "@tribally-games/tribal-token",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "prepare": "forge install && git submodule update --init --recursive && husky",
    "build": "forge build",
    "test": "forge test",
    "test-coverage": "forge coverage --report lcov",
    "devnet": "anvil",
    "commitlint": "commitlint --edit",
    "deploy-local": "forge script scripts/Deploy.s.sol --rpc-url http://localhost:8545 --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 --broadcast",
    "deploy-base-sepolia": "forge script scripts/Deploy.s.sol --rpc-url https://sepolia.base.org --chain-id 84532 --private-key $PRIVATE_KEY --broadcast",
    "deploy-base": "forge script scripts/Deploy.s.sol --rpc-url https://mainnet.base.org --chain-id 8453 --private-key $PRIVATE_KEY --broadcast"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.1",
    "@commitlint/config-conventional": "^18.6.2",
    "husky": "^9.0.11"
  }
}
